#include <math.h>
/*class madgwick {
public:
    madgwick();
private:
    float beta; //filter gain
    struct Quaternion;
    struct Eulers;
    Quaternion product(Quaternion a, Quaternion b);
    Eulers toEulers(Quaternion q);
};*/